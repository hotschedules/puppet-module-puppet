[main]
<%= @main_hash.keys.each {|k,v| "#{k} #{v}"  -%>

[agent]
<%= @agent_hash.keys.each {|k,v| "#{k} #{v}"  -%>

<% if @master %>
[master]
<%= @master_hash.keys.each {|k,v| "#{k} #{v}"  -%>
<% end %>
